name: Github Actions demo
on: [push]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Print
        run: echo "Started"
      - uses: actions/checkout@v2
      - uses: action/node@v2
      